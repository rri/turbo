//! Top-level editor and associated structures.
use std::io::Result;

/// Editor, serving as a top-level structure for working with buffers.
pub struct Editor;

impl Editor {
    /// Run the editor until the user explicitly exits.
    pub fn run() -> Result<()> {
        Ok(())
    }
}
